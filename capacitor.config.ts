import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.sprinklerbusiness.app',
  appName: 'Sprinkler Business',
  webDir: 'dist'
};

export default config;
